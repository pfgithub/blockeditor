const std = @import("std");

pub fn build(b: *std.Build) void {
    _ = b;

    // TODO:
    // running `install` runs all install steps of all packages
    // support `test` to run all tests
    // support `run-blockeditor` to run blockeditor
}