const blockdb = @import("blockdb.zig");
const blockinterface = @import("blockinterface.zig");
const text_component = @import("text_component.zig");

test {
    _ = blockdb;
    _ = blockinterface;
    _ = text_component;
}
