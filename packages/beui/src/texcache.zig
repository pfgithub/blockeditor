const std = @import("std");

// pub fn put(type: struct {})

// fn hashAnytype(value: anytype, result: *u64) void {
//     if (@hasDecl(@TypeOf(value), "hash")) value.hash();

//     // std.AutoArrayHashMap(comptime K: type, comptime V: type)
// }

// usage: put(anytype)
// it will hash based on typeid and contents
