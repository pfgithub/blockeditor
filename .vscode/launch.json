{
    "configurations": [
        {
            "type": "midas-rr",
            "request": "attach",
            "use-dap": true, // set to false if Midas stops working.
            "name": "Minimum rr",
            "trace": "Off",
        }
    ]
}