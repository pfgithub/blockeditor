{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "process",
            "label": "Build blockeditor (Debug)",
            "command": "zig",
            "args": [
                "build",
                "--prominent-compile-errors"
            ],
        }
    ]
}